Total pages: 12

--- Page 1 ---
PROMPT MASTER — DIA 3: SCROLL STORYTELLING
+ ESTÁTICA
Projeto avançado: Hero SISTOLE pronto, Attack/Anatomy/Demo/Pricing
existem
Foco: Upgrade scroll + Transformar Demo em ESTÁTICA
ESTADO ATUAL (pós-Dia 2)
JÁ EXISTE:
• Hero SISTOLE (ECG + Heart 3D + Boot Sequence + Web Audio)
• Attack section (seção existente)
• Anatomy section (features existentes)
• Demo section (input URL + resultado JSON/texto)
• Pricing + Footer
• Hooks: useKonamiCode, useTripleClick, useReducedMotion
• Componentes 3D: EnergyField, NeuralGrid
• SEO, FAQ, OG, llms.txt
PROVAVELMENTE FALTA:
• Scroll storytelling cinematográfico (transições entre seções)
• Demo ESTÁTICA (osciloscópio sonoro com 4 canais)
• Microinterações avançadas nos botões/links (fragmentação, chromatic aberration)
MISSÃO DO DIA 3: DUAS FRENTEES
FRENTE A — Scroll Storytelling (GSAP ScrollTrigger)
Transformar a navegação entre seções em uma experiência cinematográfica.
FRENTE B — Demo ESTÁTICA (Osciloscópio Sonoro)
Substituir o resultado JSON/texto da demo por um osciloscópio multi-canal com sonificação.
Tempo estimado: 4-5 horas Dependências: Dia 2 concluído Entregável: Scroll imersivo + Demo
sonora
Generated by Kimi.ai

--- Page 2 ---
FRENTE A: SCROLL STORYTELLING
PASSO 1: Análise das Seções Atuais (15 min)
Liste para mim:
1. Quais são todas as seções da landing page? (Hero, Attack, Anatomy, Demo, Pricing, Footer?)
2. Cada seção tem ID ou ref para scroll?
3. Já existe alguma animação de scroll? (fade-in básico?)
4. Qual a ordem das seções na página?
PASSO 2: Configurar GSAP ScrollTrigger Global (20 min)
No arquivo principal da página (provavelmente src/app/page.tsx ou similar):
'use client';
import { useEffect } from 'react';
import gsap from 'gsap';
import { ScrollTrigger } from 'gsap/ScrollTrigger';
gsap.registerPlugin(ScrollTrigger);
export default function HomePage() {
useEffect(() => {
// Configuração global de ScrollTrigger
ScrollTrigger.defaults({
toggleActions: 'play none none reverse',
});
// Cleanup ao desmontar
return () => {
ScrollTrigger.getAll().forEach(t => t.kill());
};
}, []);
return (
<main>
{/* Seções existentes */}
</main>
);
}
PASSO 3: Criar Transições Cinematográficas (60 min)
Transição 1: Desintegração em Pixels Quando o usuário scrolla para baixo, a seção anterior se
desintegra em pixels.
// Hook ou utilitário: usePixelDisintegration
export function usePixelDisintegration(ref: React.RefObject<HTMLElement>) {
useEffect(() => {
if (!ref.current) return;
const el = ref.current;
Generated by Kimi.ai

--- Page 3 ---
gsap.to(el, {
scrollTrigger: {
trigger: el,
start: 'bottom bottom',
end: 'bottom top',
scrub: true,
},
opacity: 0,
filter: 'blur(10px)',
scale: 0.95,
ease: 'none',
});
// Efeito de "scan" passando pela seção
const scanLine = document.createElement('div');
scanLine.style.cssText = `
position: absolute;
left: 0;
width: 100%;
height: 2px;
background: linear-gradient(90deg, transparent, #00F0FF, transparent);
z-index: 10;
opacity: 0;
`;
el.style.position = 'relative';
el.appendChild(scanLine);
gsap.to(scanLine, {
scrollTrigger: {
trigger: el,
start: 'top 80%',
end: 'bottom 20%',
scrub: true,
},
top: '100%',
opacity: 1,
ease: 'none',
});
return () => {
scanLine.remove();
};
}, [ref]);
}
Transição 2: Corte Duro (Lâmina de Luz) Nova seção emerge com clip-path ou opacity brusca.
// Em cada seção, adicione:
useEffect(() => {
if (!sectionRef.current) return;
Generated by Kimi.ai

--- Page 4 ---
gsap.fromTo(sectionRef.current,
{
clipPath: 'inset(100% 0 0 0)',
opacity: 0
},
{
scrollTrigger: {
trigger: sectionRef.current,
start: 'top 80%',
end: 'top 20%',
scrub: true,
},
clipPath: 'inset(0% 0 0 0)',
opacity: 1,
ease: 'power2.out',
}
);
}, []);
Transição 3: Parallax Inverso O fundo permanece fixo enquanto o conteúdo se move (sensação de
descida).
// No CSS da seção:
.section-parallax {
position: relative;
background-attachment: fixed; // ou use GSAP
}
// Com GSAP:
gsap.to('.section-content', {
scrollTrigger: {
trigger: '.section-container',
start: 'top bottom',
end: 'bottom top',
scrub: true,
},
y: -100, // Conteúdo se move mais rápido que o scroll
ease: 'none',
});
PASSO 4: Animar Seções Existentes (40 min)
Anatomy (Features):
• Cada feature card entra com stagger (um após o outro)
• Ícones se “desenham” no scroll (stroke-dashoffset)
• Texto aparece com fade + slide
// Em Anatomy section:
useEffect(() => {
Generated by Kimi.ai

--- Page 5 ---
const cards = gsap.utils.toArray<HTMLElement>('.feature-card');
cards.forEach((card, i) => {
gsap.fromTo(card,
{ opacity: 0, y: 50 },
{
scrollTrigger: {
trigger: card,
start: 'top 85%',
toggleActions: 'play none none reverse',
},
opacity: 1,
y: 0,
duration: 0.6,
delay: i * 0.1,
ease: 'power2.out',
}
);
});
}, []);
Attack Section:
• Se já tem animação de ataque, sincronize com scroll
• Ou deixe como está se já for interativa
Pricing:
• Cards sobem com stagger
• Guardian card tem glow que intensifica no scroll
useEffect(() => {
const cards = gsap.utils.toArray<HTMLElement>('.pricing-card');
gsap.fromTo(cards,
{ opacity: 0, y: 80 },
{
scrollTrigger: {
trigger: '.pricing-section',
start: 'top 70%',
},
opacity: 1,
y: 0,
duration: 0.8,
stagger: 0.15,
ease: 'power3.out',
}
);
}, []);
Generated by Kimi.ai

--- Page 6 ---
FRENTE B: DEMO ESTÁTICA (Osciloscópio Sonoro)
PASSO 1: Análise da Demo Atual (15 min)
Liste para mim:
1. Qual arquivo é a Demo atual?
2. O que ela renderiza? (input URL, botão scan, área de resultado?)
3. O resultado atual é JSON/texto ou já tem alguma visualização?
4. Qual endpoint de scan ela chama?
PASSO 2: Criar Oscilloscope.tsx (60 min)
Arquivo: src/components/oscilloscope/Oscilloscope.tsx
'use client';
import { useRef, useEffect, useCallback } from 'react';
interface Channel {
method: 'GET' | 'POST' | 'PUT' | 'DELETE';
color: string;
frequency: number; // Hz para som
}
const CHANNELS: Channel[] = [
{ method: 'GET', color: '#00FF88', frequency: 440 },
{ method: 'POST', color: '#0088FF', frequency: 523 },
{ method: 'PUT', color: '#FFBB00', frequency: 659 },
{ method: 'DELETE', color: '#FF003C', frequency: 880 },
];
interface OscilloscopeProps {
isScanning: boolean;
threatDetected: boolean;
onAudioInit?: () => void;
}
export default function Oscilloscope({ isScanning, threatDetected, onAudioInit }: OscilloscopeProps) {
const canvasRef = useRef<HTMLCanvasElement>(null);
const audioCtxRef = useRef<AudioContext | null>(null);
const oscillatorsRef = useRef<OscillatorNode[]>([]);
const gainNodesRef = useRef<GainNode[]>([]);
const analyserRef = useRef<AnalyserNode | null>(null);
const dataArrayRef = useRef<Uint8Array | null>(null);
const animationRef = useRef<number>(0);
const phaseRef = useRef(0);
// Inicializar áudio
const initAudio = useCallback(() => {
if (audioCtxRef.current) return;
Generated by Kimi.ai

--- Page 7 ---
const ctx = new AudioContext();
audioCtxRef.current = ctx;
// Analyser para visualização
const analyser = ctx.createAnalyser();
analyser.fftSize = 256;
analyserRef.current = analyser;
dataArrayRef.current = new Uint8Array(analyser.frequencyBinCount);
// Criar osciladores para cada canal
CHANNELS.forEach((ch, i) => {
const osc = ctx.createOscillator();
const gain = ctx.createGain();
osc.type = 'sine';
osc.frequency.setValueAtTime(ch.frequency, ctx.currentTime);
gain.gain.setValueAtTime(0, ctx.currentTime); // Começa mudo
osc.connect(gain);
gain.connect(analyser);
analyser.connect(ctx.destination);
osc.start();
oscillatorsRef.current[i] = osc;
gainNodesRef.current[i] = gain;
});
onAudioInit?.();
}, [onAudioInit]);
// Atualizar som baseado no estado
useEffect(() => {
if (!audioCtxRef.current) return;
const ctx = audioCtxRef.current;
if (isScanning) {
// Aumentar volume de todos os canais
gainNodesRef.current.forEach((gain, i) => {
const targetGain = threatDetected ? 0.08 : 0.05;
gain.gain.setTargetAtTime(targetGain, ctx.currentTime, 0.1);
// Se threat, adicionar distorção (mudar para sawtooth)
if (threatDetected) {
oscillatorsRef.current[i].type = 'sawtooth';
// Detune para criar dissonância
oscillatorsRef.current[i].detune.setValueAtTime(
(Math.random() - 0.5) * 100,
ctx.currentTime
);
Generated by Kimi.ai

--- Page 8 ---
} else {
oscillatorsRef.current[i].type = 'sine';
oscillatorsRef.current[i].detune.setValueAtTime(0, ctx.currentTime);
}
});
} else {
// Mudo quando não está scannando
gainNodesRef.current.forEach(gain => {
gain.gain.setTargetAtTime(0, ctx.currentTime, 0.3);
});
}
}, [isScanning, threatDetected]);
// Renderização do canvas
useEffect(() => {
const canvas = canvasRef.current;
if (!canvas) return;
const ctx = canvas.getContext('2d');
if (!ctx) return;
const resize = () => {
canvas.width = canvas.offsetWidth * window.devicePixelRatio;
canvas.height = canvas.offsetHeight * window.devicePixelRatio;
ctx.scale(window.devicePixelRatio, window.devicePixelRatio);
};
resize();
window.addEventListener('resize', resize);
const animate = () => {
const w = canvas.offsetWidth;
const h = canvas.offsetHeight;
const channelHeight = h / 4;
ctx.fillStyle = '#0A0A0F';
ctx.fillRect(0, 0, w, h);
// Grid
ctx.strokeStyle = 'rgba(0, 240, 255, 0.03)';
ctx.lineWidth = 1;
for (let i = 1; i < 4; i++) {
const y = i * channelHeight;
ctx.beginPath();
ctx.moveTo(0, y);
ctx.lineTo(w, y);
ctx.stroke();
}
phaseRef.current += 0.05;
CHANNELS.forEach((ch, i) => {
const centerY = i * channelHeight + channelHeight / 2;
Generated by Kimi.ai

--- Page 9 ---
const amplitude = isScanning ? (threatDetected ? 25 : 15) : 5;
const freq = 0.02 + i * 0.005;
ctx.beginPath();
ctx.strokeStyle = ch.color;
ctx.lineWidth = 2;
ctx.shadowBlur = isScanning ? 15 : 5;
ctx.shadowColor = ch.color;
for (let x = 0; x < w; x += 2) {
const y = centerY + Math.sin(x * freq + phaseRef.current + i) * amplitude;
if (x === 0) ctx.moveTo(x, y);
else ctx.lineTo(x, y);
}
ctx.stroke();
ctx.shadowBlur = 0;
// Label do método
ctx.fillStyle = ch.color;
ctx.font = '10px JetBrains Mono';
ctx.fillText(ch.method, 10, centerY - 20);
});
// Barra de filtro (sweep) quando scanning
if (isScanning) {
const sweepX = (phaseRef.current * 50) % (w + 100) - 50;
const gradient = ctx.createLinearGradient(sweepX - 30, 0, sweepX + 30, 0);
gradient.addColorStop(0, 'transparent');
gradient.addColorStop(0.5, 'rgba(157, 0, 255, 0.3)');
gradient.addColorStop(1, 'transparent');
ctx.fillStyle = gradient;
ctx.fillRect(sweepX - 30, 0, 60, h);
// Linha vertical roxa
ctx.strokeStyle = '#9D00FF';
ctx.lineWidth = 2;
ctx.shadowBlur = 20;
ctx.shadowColor = '#9D00FF';
ctx.beginPath();
ctx.moveTo(sweepX, 0);
ctx.lineTo(sweepX, h);
ctx.stroke();
ctx.shadowBlur = 0;
}
animationRef.current = requestAnimationFrame(animate);
};
animate();
return () => {
cancelAnimationFrame(animationRef.current);
Generated by Kimi.ai

--- Page 10 ---
window.removeEventListener('resize', resize);
};
}, [isScanning, threatDetected]);
return (
<div className="relative w-full">
<canvas
ref={canvasRef}
className="w-full h-[300px] cursor-pointer rounded border border-secondary/20"
onClick={initAudio}
/>
{!audioCtxRef.current && (
<div className="absolute inset-0 flex items-center justify-center bg-bg/80">
<p className="font-mono text-sm text-secondary">{'[ CLIQUE PARA ATIVAR ÁUDIO ]'}</p>
</div>
)}
</div>
);
}
PASSO 3: Modificar Demo Existente (45 min)
NÃO delete a demo atual. Modifique-a para:
1. Manter input de URL
2. Manter botão de scan
3. SUBSTITUIR a área de resultado JSON/texto pelo Oscilloscope
4. Integrar com API real existente
// Na demo existente:
const [isScanning, setIsScanning] = useState(false);
const [threatDetected, setThreatDetected] = useState(false);
const [audioInitialized, setAudioInitialized] = useState(false);
const handleScan = async (url: string) => {
setIsScanning(true);
setThreatDetected(false);
try {
const res = await fetch(`${process.env.NEXT_PUBLIC_API_URL}/v1/scan`, {
method: 'POST',
headers: { 'Content-Type': 'application/json' },
body: JSON.stringify({ url }),
});
const data = await res.json();
// Se encontrou ameaça
setThreatDetected(data.threats?.length > 0 || !data.safe);
// Manter scanning por 3 segundos para efeito visual
Generated by Kimi.ai

--- Page 11 ---
setTimeout(() => {
setIsScanning(false);
}, 3000);
} catch {
setThreatDetected(true);
setTimeout(() => setIsScanning(false), 3000);
}
};
// No JSX:
<div className="demo-section">
<input
value={url}
onChange={e => setUrl(e.target.value)}
placeholder="https://exemplo.com"
className="bg-bg border border-secondary/30 text-text font-mono p-3 w-full"
/>
<button
onClick={() => handleScan(url)}
disabled={isScanning}
className="border border-secondary text-secondary font-mono px-6 py-3
hover:bg-secondary hover:text-bg transition-all disabled:opacity-50"
>
{isScanning ? '> SCANNING...' : '> INICIAR_SCAN'}
</button>
{/* SUBSTITUIR resultado JSON por: */}
<Oscilloscope
isScanning={isScanning}
threatDetected={threatDetected}
onAudioInit={() => setAudioInitialized(true)}
/>
{/* Resultado textual opcional (mantenha se quiser) */}
{!isScanning && result && (
<div className="mt-4 font-mono text-sm">
{threatDetected ? (
<span className="text-primary">{'[ AMEAÇA DETECTADA ]'}</span>
) : (
<span className="text-secondary">{'[ LIMPO — 21ms ]'}</span>
)}
</div>
)}
</div>
PASSO 4: Teste e Ajuste (30 min)
1. Verifique se scroll entre seções é fluído
2. Verifique se osciloscópio renderiza 4 canais
3. Verifique se som funciona (requer clique para inicializar áudio)
Generated by Kimi.ai

--- Page 12 ---
4. Verifique se scan real chama API e atualiza visualização
5. Verifique se não quebrou nada existente
PASSO 5: Commit (5 min)
git add .
git commit -m "feat: Dia 3 — Scroll storytelling + Demo ESTÁTICA (osciloscópio sonoro)"
REGRAS ABSOLUTAS
1. NÃO delete seções existentes — apenas adicione animações
2. NÃO delete a lógica de scan da API — apenas mude a visualização
3. NÃO delete input de URL ou botão de scan existentes
4. Web Audio API só inicializa após interação do usuário
5. GSAP ScrollTrigger deve ter cleanup no useEffect return
6. Teste prefers-reduced-motion — se ativo, desative animações complexas de scroll
RELATÓRIO DE ENTREGA
DIA 3 CONCLUÍDO — SCROLL + ESTÁTICA
Scroll Storytelling:
- GSAP ScrollTrigger configurado globalmente
- Transições: desintegração em pixels, corte duro (clip-path), scanline
- Seções animadas: Hero (fade), Anatomy (stagger cards), Pricing (stagger cards)
- Parallax: conteúdo se move mais rápido que scroll
Demo ESTÁTICA:
- Osciloscópio Canvas 2D com 4 canais:
* GET: verde (#00FF88), 440Hz
* POST: azul (#0088FF), 523Hz
* PUT: amarelo (#FFBB00), 659Hz
* DELETE: vermelho (#FF003C), 880Hz
- Web Audio API: osciladores sine, volume controlado
- Threat = distorção (sawtooth + detune)
- Barra roxa-luminosa (#9D00FF) sweep durante scan
- Inicializa áudio após clique
Build: PASSOU / FALHOU
Commit: feat: Dia 3 — Scroll storytelling + Demo ESTÁTICA
Próximo passo: Dia 4 — Microinterações + Easter Eggs + Pricing Glow
Execute agora. Não pare para perguntar. Reporte ao final.
Generated by Kimi.ai
